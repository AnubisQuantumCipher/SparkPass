project Test_MLKEM is
   for Languages use ("Ada");
   for Source_Dirs use (
      "src/sparkpass",
      "src/sparkpass/crypto");
   for Object_Dir use "obj/mlkem_test";

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2020", "-gnatc");
   end Compiler;

   package Prove is
      for Proof_Switches ("Ada") use ("--level=0", "--timeout=10");
   end Prove;

end Test_MLKEM;
